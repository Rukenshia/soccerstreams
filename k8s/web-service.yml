apiVersion: v1
kind: Service
metadata:
  name: soccerstreams-web-service
  labels:
    run: soccerstreams-web-service
spec:
  type: LoadBalancer
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: soccerstreams-web
